# Development/CI tooling for `make lint` and `make test`.
# Install into a local venv via `make lint-install` (creates .venv-dev/).
#
# Pin to known-working versions; bump deliberately after local green run.

ansible-core==2.17.5
ansible-lint==24.9.2
yamllint==1.35.1
jinja2==3.1.4
ruff==0.15.9
